mutation UpdateUserImage($id: Int!, $pictureURL: String!) {
  EditUser(id: $id, profilePictureURL: $pictureURL) {
    code
    success
    message
    User {
      id
      firstName
      profilePictureURL
    }
  }
}
